apiVersion: v1
kind: Service
metadata:
  name: java-app-service
  namespace: production
spec:
  selector:
    app: java-app
    version: blue   # Initially pointing to blue
  ports:
    - port: 80
      targetPort: 8080